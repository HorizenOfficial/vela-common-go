# Horizen Vela Common Go Library

Shared Go types and utilities for the Horizen Vela platform. Provides common framework types, a GraphQL subgraph client, and WASM guest-side primitives (256-bit integers, addresses, memory management, logging) used by multiple downstream services.